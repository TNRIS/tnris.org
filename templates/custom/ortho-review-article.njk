{% extends "base.njk" %}
{% block topcontent %}
<div class="container-fluid full">
  <div class="top-container">
      {% if mainimage %}
        <img class="img-fluid mx-auto d-block d-none d-sm-none" src="{{mainimage}}" alt="main image for {{title}}">
        <img class="img-fluid mx-auto d-block d-lg-none d-md-none" src="{{mainimagemobile}}" alt="main image for {{title}}">
      {% endif %}


      <div class="top-header mx-auto d-block">
          <div class="container">
            <h1>{{title}}</h1>
            {% if abstract %}
              <div class="lead">{{abstract}}</div>
            {% endif %}
          </div>
      </div>
      </div>
</div>
{% endblock %}

{% block contentsTwo %}

{{ contents|safe }} 

{% endblock %}


</div>




